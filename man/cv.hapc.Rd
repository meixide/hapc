% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.hapc.R
\name{cv.hapc}
\alias{cv.hapc}
\title{Cross-validated HAPC fit}
\usage{
cv.hapc(
  X,
  Y,
  family = c("gaussian", "binomial", "logit-hazard"),
  max_degree = 1L,
  npcs = nrow(X),
  log_lambda_min = -5,
  log_lambda_max = -3,
  grid_length = 10L,
  nfolds = 5L,
  norm = c("sv", "1", "2"),
  predict = NULL,
  max_iter = 5000L,
  tol = 0.001,
  step_factor = 0.8,
  verbose = FALSE,
  crit = c("grad", "risk"),
  center = TRUE,
  approx = FALSE,
  ini = c("1", "2"),
  Delta = NULL,
  time_grid = NULL
)
}
\arguments{
\item{X}{Numeric matrix of features (rows = observations, cols = features).}

\item{Y}{Numeric response vector of length \code{nrow(X)}. For
\code{family = "binomial"}: hard labels in \code{{0,1}} or \code{{-1,+1}},
or \emph{soft} labels in \code{[0,1]} (e.g. EM-HAL E-step posteriors).
Soft labels are supported only for \code{norm = "1"} or \code{"2"}
(cross-entropy target); \code{norm = "sv"} requires hard labels.}

\item{family}{Character: \code{"gaussian"}, \code{"binomial"}, or
\code{"logit-hazard"}.  The last is a discrete-time \emph{logistic hazard}
model: the second argument \code{Y} is then the observed time
\eqn{T = \min(T^{event}, C)} and \code{Delta} (event indicator) must be
supplied; the call is forwarded to \code{\link{hazard.hapc}}.}

\item{max_degree}{Integer; maximum interaction order for the HAL basis.
Default \code{1L} (additive HAL).}

\item{npcs}{Integer; number of principal components to keep. Default
\code{nrow(X)}.}

\item{log_lambda_min}{Numeric; lower bound of the log-lambda grid. Default
\code{-5}.}

\item{log_lambda_max}{Numeric; upper bound of the log-lambda grid. Default
\code{-3}.}

\item{grid_length}{Integer; number of grid points. Default \code{10L}.}

\item{nfolds}{Integer; number of CV folds. Default \code{5L}.}

\item{norm}{Character: \code{"sv"}, \code{"1"} or \code{"2"}. Default
\code{"sv"}. For \code{family = "binomial"} all three are supported and
the loss is always logistic: \code{"sv"} = PGD on logistic loss with
sectional-variation projection, \code{"2"} = logistic ridge (Newton-
Raphson IRLS, no PGD), \code{"1"} = logistic LASSO via
\code{glmnet::glmnet(..., family = "binomial", alpha = 1,
intercept = FALSE)} on \eqn{\tilde{X} = U \cdot \mathrm{diag}(d)}
(requires the \code{glmnet} package).}

\item{predict}{Optional numeric matrix of new observations (same number of
columns as \code{X}). If supplied, predictions are returned in the result.}

\item{max_iter}{Integer; maximum projected-gradient iterations (only used
when \code{norm = "sv"}). Default \code{5000L}.}

\item{tol}{Numeric; convergence tolerance (only used when
\code{norm = "sv"}). Default \code{1e-3}.}

\item{step_factor}{Numeric; PGD line-search factor (only used when
\code{norm = "sv"}). Default \code{0.8}.}

\item{verbose}{Logical; print progress (only used when \code{norm = "sv"}).
Default \code{FALSE}.}

\item{crit}{Character; PGD stopping criterion (only used when
\code{norm = "sv"}): \code{"grad"} (gradient infinity-norm, default) or
\code{"risk"} (relative risk decrease).}

\item{center}{Logical; whether to centre the kernel matrix and response.
Default \code{TRUE}.}

\item{approx}{Logical; if \code{TRUE} use approximate eigendecomposition
(power iteration) for \code{norm \%in\% c("1","2")}. Ignored for
\code{norm = "sv"}. Default \code{FALSE}.}

\item{ini}{Character; initialiser for the projected-gradient solver when
\code{norm = "sv"}: \code{"1"} = LASSO via \code{fast_pchal_call}
(default, matches Python), \code{"2"} = ridge via \code{ridge_call}.}

\item{Delta}{Event indicator \eqn{\Delta \in \{0,1\}} of length
\code{nrow(X)}, required only when \code{family = "logit-hazard"}.
Ignored otherwise.}

\item{time_grid}{Optional discrete time grid for
\code{family = "logit-hazard"} (see \code{\link{hazard.hapc}}). Ignored
otherwise.}
}
\value{
A list with elements \code{mses} / \code{deviances} (binomial),
  \code{lambdas}, \code{best_lambda}, \code{res_opt} (refit on full data),
  and \code{predictions} when \code{predict} is non-NULL.
}
\description{
k-fold cross-validation over a log-spaced grid of \code{lambda} values for
the Highly Adaptive Principal Components (HAPC) model, supporting all three
norm constraints (\code{"sv"}, \code{"1"}, \code{"2"}) and both
\code{"gaussian"} and \code{"binomial"} families.
}
\details{
Mirror of the Python function \code{hapc.cv_hapc()}: identical argument
names, defaults, fold partitioning (\code{std::mt19937(12345)} + shuffle),
and per-fold optimisation pipeline.


Fold construction is implemented in C++: block partition (remainder in the
last fold), then a shuffle with fixed seed \code{12345}
(\code{std::mt19937}), identical to Python \code{cv_hapc()}.

Loss-by-family contract (binary responses are \emph{always} fit and scored
with logistic loss):
\describe{
  \item{\code{family = "gaussian"}}{\code{norm = "sv"} → ridge/LASSO init +
    PGD on squared error per fold; \code{norm \%in\% c("1","2")} → closed
    form LASSO/ridge in PC basis, MSE per fold.}
  \item{\code{family = "binomial"}, \code{norm = "sv"}}{Logistic ridge init +
    projected gradient descent on logistic loss per fold; logistic deviance
    scoring.}
  \item{\code{family = "binomial"}, \code{norm = "2"}}{Logistic ridge only
    (no PGD) per fold; logistic deviance scoring.}
  \item{\code{family = "binomial"}, \code{norm = "1"}}{Logistic LASSO via
    \code{glmnet} (\code{family = "binomial"}, \code{alpha = 1},
    \code{intercept = FALSE}) on \eqn{\tilde{X} = U \cdot
    \mathrm{diag}(d)} per fold; logistic deviance scoring. Requires the
    \code{glmnet} package (in \code{Suggests}).}
}
}
\examples{
\dontrun{
n <- 200; d <- 3
X <- matrix(runif(n * d, 0, 1), ncol = d)
Y <- sin(pi * X[, 1]) + X[, 2] + rnorm(n, sd = 0.1)
cv <- cv.hapc(X, Y, max_degree = 2, npcs = n - 1, nfolds = 5,
              grid_length = 10, norm = "sv")
}

}
