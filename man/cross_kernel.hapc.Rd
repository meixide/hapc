% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_kernel.hapc.R
\name{cross_kernel.hapc}
\alias{cross_kernel.hapc}
\title{Cross kernel matrix between train and test points}
\usage{
cross_kernel.hapc(X, Xte, max_degree = 1L, center = TRUE)
}
\arguments{
\item{X}{Numeric matrix of training features (used to define the basis).}

\item{Xte}{Numeric matrix of test features. Must have the same number of
columns as \code{X}.}

\item{max_degree}{Integer; maximum interaction order (must match the value
used to fit). Default \code{1L}.}

\item{center}{Logical; whether to apply the same column centring used in
training. Default \code{TRUE}.}
}
\value{
An \eqn{n_{te} \times n_{tr}} numeric matrix.
}
\description{
Computes the cross-kernel \eqn{K_{te,tr} = H_{te} H_{tr}^\top} of the HAL
basis induced by \code{X} (train) at the points \code{Xte} (test). Used
under the hood by HAPC predictions.
}
\details{
Counterpart to Python \code{hapc.cross_kernel_hapc()}.
}
\examples{
\dontrun{
X  <- matrix(rnorm(100 * 5), 100, 5)
Xn <- matrix(rnorm( 20 * 5),  20, 5)
Kx <- cross_kernel.hapc(X, Xn, max_degree = 1)
}

}
