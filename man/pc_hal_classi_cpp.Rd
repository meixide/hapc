% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcghal_classi.R
\name{pc_hal_classi_cpp}
\alias{pc_hal_classi_cpp}
\title{Direct PC-HAL classification optimiser (precomputed basis)}
\usage{
pc_hal_classi_cpp(
  Y,
  Xtilde,
  E_Nn,
  alpha,
  max_iter = 5000L,
  tol = 0.001,
  step_factor = 0.8,
  verbose = FALSE
)
}
\arguments{
\item{Y}{Numeric \{-1, +1\} response vector.}

\item{Xtilde}{\eqn{n \times k} matrix \eqn{U D} (left singular vectors scaled
by the top-\eqn{k} singular values).}

\item{E_Nn}{\eqn{B \times k} matrix \eqn{V} from \code{\link{design.hapc}}
(right singular vectors; \eqn{B} is the HAL basis dimension).}

\item{alpha}{Numeric vector of initial coefficients.}

\item{max_iter}{Integer; maximum PGD iterations. Default \code{5000L}.}

\item{tol}{Numeric convergence tolerance. Default \code{1e-3}.}

\item{step_factor}{Numeric line-search factor. Default \code{0.8}.}

\item{verbose}{Logical; print progress. Default \code{FALSE}.}
}
\value{
A list with \code{alpha}, \code{alphaiters}, \code{beta},
  \code{risk}, and \code{iter}.
}
\description{
Runs projected gradient descent for the binomial / logistic-loss HAPC
problem given pre-computed PC ingredients (\code{Xtilde = U D},
\code{E_Nn = V}) and an initial \eqn{\alpha_0}.  Mirrors Python's
\code{hapc.pcghal_classification()}.
}
\details{
Useful when the design has already been built (e.g. when iterating across
many lambdas while keeping \eqn{H} fixed).
}
\examples{
\dontrun{
n <- 80; p <- 3
X <- matrix(rnorm(n * p), nrow = n, ncol = p)
Y <- sample(c(-1, 1), n, replace = TRUE)
des <- design.hapc(X, max_degree = 2, npcs = 10)
k <- min(10L, length(des$d))
Xtilde <- des$U[, 1:k, drop = FALSE] * rep(des$d[1:k], each = nrow(des$U))
E_Nn <- des$V[, 1:k, drop = FALSE]
a0 <- rep(0, k)
fit <- pc_hal_classi_cpp(Y, Xtilde, E_Nn, a0, max_iter = 100, tol = 1e-2)
str(fit)
}

}
